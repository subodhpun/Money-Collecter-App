import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sahakari.gurudev',
  appName: 'gurudev-sahakari',
  webDir: 'public'
};

export default config;
